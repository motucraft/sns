enum UploadType {
  gallery,
  camera,
}

enum PostCaptionOpenMode {
  fromPost,
  fromFeed,
}

enum FeedMode {
  // 自分+フォローしているユーザ
  fromFeed,
  // プロフィール画面に表示されているユーザのみ
  fromProfile,
}

enum PostMenu {
  edit,
  delete,
  share,
}

enum ProfileMode {
  myself,
  other,
}

enum ProfileSettingMenu {
  themeChange,
  signOut,
}

enum WhoCaresMeMode {
  like,
  followings,
  followed,
}
